## Navigation (alt - ...)
# Space Navigation: alt - {1, 2, 3, 4, 0}
alt - 1 : yabai -m space --focus 1
alt - 2 : yabai -m space --focus 2
alt - 3 : yabai -m space --focus 3
alt - 4 : yabai -m space --focus 4
alt - 5 : yabai -m space --focus 5
alt - 0 : yabai -m space --focus recent

# Window Navigation: alt - {h, j, k, l, o}
alt - h    : yabai -m window --focus west
alt - j    : yabai -m window --focus south
alt - k    : yabai -m window --focus north
alt - l    : yabai -m window --focus east
alt - m    : yabai -m window --focus recent

# Rotate tree
alt + ctrl - r : yabai -m space --rotate 90

# Equalize size of windows
alt + ctrl - e : yabai -m space --balance

# Enable / Disable gaps in current workspace
alt + ctrl - g : yabai -m space --toggle padding; yabai -m space --toggle gap

## Misc (alt - ...)
# Open terminal
alt - return : /Applications/Kitty.app/Contents/MacOS/kitty --single-instance -d ~ &> /dev/null

# Open mpv with url from clipboard
# alt - m : open -na /Applications/mpv.app $(pbpaste)


# Extended Window Navigation: alt - {h, ä}
# alt -    h : yabai -m window --focus first
# alt - 0x27 : yabai -m window --focus  last

# Make window zoom to fullscreen: alt + ctrl - f
alt + ctrl - f : yabai -m window --toggle zoom-fullscreen; sketchybar --trigger window_focus

## Window Movement (alt + ctrl - ...)
# Moving windows in spaces: alt + ctrl - {h, j, k, l}
alt + ctrl - h : yabai -m window --swap west
alt + ctrl - j : yabai -m window --swap south
alt + ctrl - k : yabai -m window --swap north
alt + ctrl - l : yabai -m window --swap east

# Moving windows between spaces: alt + ctrl - {1, 2, 3, 4, p, n }
alt + ctrl - 1 : yabai -m window --space 1
alt + ctrl - 2 : yabai -m window --space 2
alt + ctrl - 3 : yabai -m window --space 3
alt + ctrl - 4 : yabai -m window --space 4
alt + ctrl - p : yabai -m window --space prev; yabai -m space --focus prev
alt + ctrl - n : yabai -m window --space next; yabai -m space --focus next

# Mirror Space on X and Y Axis: alt + ctrl - {x, y}
# alt + ctrl - x : yabai -m space --mirror x-axis
# alt + ctrl - y : yabai -m space --mirror y-axis


## Resize (alt + shift - ...)
# Resize windows: alt + shift - {j, k, l, ö}
alt + shift - h    : yabai -m window --resize left:-50:0;  yabai -m window --resize right:-50:0
alt + shift - j    : yabai -m window --resize bottom:0:50; yabai -m window --resize top:0:50
alt + shift - k    : yabai -m window --resize top:0:-50;   yabai -m window --resize bottom:0:-50
alt + shift - l : yabai -m window --resize right:50:0;  yabai -m window --resize left:50:0
